#include "main.h"
/**
 *_abs - main function
 *
 *@c: is an integer to make absolute
 *
 *Return: int or int multiplied by negative
 */
int _abs(int c)
{
if (c > 0)
return (c);
else
return (c * -1);
}
