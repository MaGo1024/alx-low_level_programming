#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 *_strlen - function to find lenght of paramater value s
 *@s: pointer value of character
 * Return: lenght of string.
 */
int _strlen(char *s)
{
strlen(s);
}
